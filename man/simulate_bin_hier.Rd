% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_bin_hier.R
\name{simulate_bin_hier}
\alias{simulate_bin_hier}
\title{Simulate Hierarchical Data with a Binary Response}
\usage{
simulate_bin_hier(
  length_X1,
  length_X2,
  formula_X1,
  formula_Y,
  para_X1,
  beta,
  n_clus,
  n_obs,
  sd_U,
  seed = NULL
)
}
\arguments{
\item{length_X1}{Integer. Number of levels for the ordered factor X1 (must be >= 2).}

\item{length_X2}{Integer. Number of levels for the unordered factor X2 (must be between 2 and 26).}

\item{formula_X1}{Character. Formula specifying how X1 depends on X2.}

\item{formula_Y}{Character. Formula specifying how Y depends on X1 and X2.}

\item{para_X1}{Matrix. Coefficient matrix for modeling X1 given X2.
Must have \code{length_X1} rows and \code{(length_X2 - 1)} columns.}

\item{beta}{Numeric vector. Coefficients for modeling the binary outcome Y.
Must match the number of columns of the model matrix generated by \code{formula_Y}.}

\item{n_clus}{Integer. Number of clusters (e.g., groups, hospitals).}

\item{n_obs}{Integer. Number of observations per cluster.}

\item{sd_U}{Numeric. Standard deviation of the random effects at the cluster level.}

\item{seed}{Integer or NULL. Random seed for reproducibility (default is NULL).}
}
\value{
A \code{data.frame} with the following columns:
\describe{
\item{id}{Integer identifier for each observation}
\item{Y}{Binary outcome (factor: 0 or 1)}
\item{X1}{Ordered factor with \code{length_X1} levels}
\item{X2}{Unordered factor with \code{length_X2} alphabetic levels (a, b, c, ...)}
\item{clus}{Cluster identifier (factor)}
}
}
\description{
Simulates hierarchical binary data where observations are grouped into clusters,
with an ordered covariate (X1) and an unordered covariate (X2), and random effects
at the cluster level.
}
\examples{
dat2 <- simulate_bin_hier(
  length_X1 = 3,
  length_X2 = 4,
  formula_X1 = "~ X2",
  formula_Y = "~ X1 + X2",
  para_X1 = matrix(c(rep(2,3), rep(1,3), rep(2.5,3)), ncol = 3),
  beta = c(-1, 1, -2, 2, 1, 2),
  n_clus = 10,
  n_obs = 200,
  sd_U = 0.45,
  seed = 123
)
head(dat2)
}
